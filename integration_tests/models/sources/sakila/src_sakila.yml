version: 2

sources:
  - name: seeds
    tables:
      - name: customer
        columns:
          - name: first_name
            meta:
                masking_policy: mp_encrypt_pii
        meta:
          row_access_policy: ra_hidden_customer
          row_access_policy_columns: 
            - customer_id
            - address_id
