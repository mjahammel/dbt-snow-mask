version: 2

sources:
  - name: seeds
    tables:
      - name: customer
        columns:
          - name: first_name
            meta:
                masking_policy: mp_encrypt_pii
    meta:
      row_access_policy: hide_random
      row_access_policy_columns: 
        - address_id
        - customer_id
