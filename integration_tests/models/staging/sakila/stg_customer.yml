version: 2

models:
  - name: stg_customer
    columns:
      - name: email
        meta:
          masking_policy: mp_encrypt_pii
    meta:
      row_access_policy: hide_random
      row_access_policy_columns:
        - address_id 
        - customer_id 
